%{
#include "hash.h"

%}

%x POST

%%

("\<li class=\"post\"")                 {printf("Hello\n");}



.|\n    {;}


%%
int yywrap(){ return(1); }

int main(){ 
       // Hash * hash = (Hash *)malloc(sizeof(Hash));

       // init(*hash);
       // displayHash(*hash);
        
        yylex(); 
        return 0; 
}

